{
  "name": "@heliopause/sandbox",
  "version": "0.1.0",
  "description": "A browser-contained virtual filesystem and limited command sandbox.",
  "type": "module",
  "private": false,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --loader ./test/ts-extension-loader.mjs --test test/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
